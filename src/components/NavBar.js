import React from "react";

const NavBar = () => {
  return (
    <section>
      <header>
        <section>logo</section>
        <section>menu</section>
      </header>
    </section>
  );
};

export default NavBar;
